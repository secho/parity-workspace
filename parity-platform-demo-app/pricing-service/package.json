{
  "name": "parityshop-pricing-service",
  "private": true,
  "type": "module",
  "description": "The migration target: sp_CalculateOrderTotal as a service. M5 writes it by hand so the shadow harness has two implementations to compare; M6 replaces this source with the agent's.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "fastify": "^5.2.0",
    "mssql": "^11.0.1"
  },
  "devDependencies": {
    "@types/mssql": "^12.3.0",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
